#-------------------------------------------------------------------------------
#-------------------------------------------------------------------------------
Pos<-function(l,lmax){
   U<--lmax:lmax
   u<-U*0
   u[lmax+l+1]<-1
   return(data.frame(idx=seq(0,2*lmax),U,pos=u))   
}
